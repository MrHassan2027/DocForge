[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "docforge"
version = "1.0.0"
description = "AST-based Python doc generator, zero deps"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
docforge = "docforge.parser:main"
